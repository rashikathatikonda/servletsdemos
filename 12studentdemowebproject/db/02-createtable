use studentweb;
create table student(
             id int  AUTO_INCREMENT PRIMARY_KEY,
             first_name varchar(45),
             last_name varchar(45),
             email varchar(45)
             );
